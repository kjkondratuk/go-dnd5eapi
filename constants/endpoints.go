package constants

const (
	// Standard delimiter for URLs
	Slash = "/"

	RootEndpoint = ""

	// Endpoints for conditions
	ConditionsEndpoint      = "/conditions"
	ConditionsChildEndpoint = ConditionsEndpoint + Slash

	// Endpoints for damage types
	DamageTypesEndpoint      = "/damage-types"
	DamageTypesChildEndpoint = DamageTypesEndpoint + Slash

	// Endpoints for equipment categories
	EquipmentCategoriesEndpoint      = "/equipment-categories"
	EquipmentCategoriesChildEndpoint = EquipmentCategoriesEndpoint + Slash

	// Endpoints for equipment
	EquipmentEndpoint      = "/equipment"
	EquipmentChildEndpoint = EquipmentEndpoint + Slash

	// Endpoints for features
	FeaturesEndpoint      = "/features"
	FeaturesChildEndpoint = FeaturesEndpoint + Slash

	// Endpoints for magic schools
	MagicSchoolsEndpoint      = "/magic-schools"
	MagicSchoolsChildEndpoint = MagicSchoolsEndpoint + Slash

	// Endpoints for monsters
	MonstersEndpoint      = "/monsters"
	MonstersChildEndpoint = MonstersEndpoint + Slash

	// Endpoints for races
	RacesEndpoint      = "/races"
	RacesChildEndpoint = RacesEndpoint + Slash

	// Endpoints for spellcasting
	SpellcastingEndpoint      = "/spellcasting"
	SpellcastingChildEndpoint = SpellcastingEndpoint + Slash

	// Endpoints for spells
	SpellsEndpoint      = "/spells"
	SpellsChildEndpoint = SpellsEndpoint + Slash

	// Endpoints for starting-equipment
	StartingEquipmentEndpoint      = "/starting-equipment"
	StartingEquipmentChildEndpoint = StartingEquipmentEndpoint + Slash

	// Endpoints for subraces
	SubracesEndpoint     = "/subraces"
	SubracesChildEnpoint = SubracesEndpoint + Slash

	// Endpoints for traits
	TraitsEndpoint      = "/traits"
	TraitsChildEndpoint = TraitsEndpoint + Slash

	// Endpoints for weapon-properties
	WeaponPropertiesEndpoint      = "/weapon-properties"
	WeaponPropertiesChildEndpoint = WeaponPropertiesEndpoint + Slash
)
